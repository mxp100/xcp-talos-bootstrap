# Talos control plane machineconfig
version: v1alpha1
machine:
  type: controlplane
  token: "REPLACE_WITH_CLUSTER_TOKEN"
  install:
    image: factory.talos.dev/nocloud-installer/af0f260ca05688ef5c94894566b3b3c73a35ad272f64a8c3e5b0e48e0a0cac6a:v1.11.3
    disk: /dev/xvda
    wipe: true
  time:
    servers:
      - pool.ntp.org
  network:
    hostname:
    interfaces:
      - interface: eth0
        dhcp: false

cluster:
  id: "REPLACE_WITH_CLUSTER_ID"        # talosctl gen config создаёт автоматически; можно оставить свой UUID
  secret: "REPLACE_WITH_CLUSTER_SECRET"
  clusterName: "talos-xcp"
  controlPlane:
    endpoint: "https://192.168.10.2:6443"   # будет перезаписан скриптом, можно оставить так
  network:
    cni:
      name: none
    podSubnets:
      - 10.244.0.0/16
    serviceSubnets:
      - 10.96.0.0/12
  apiServer:
    certSANs:
      - 192.168.10.2
      - 192.168.10.3
      - 192.168.10.4
    extraArgs:
      service-account-issuer: kubernetes.default.svc
      service-account-jwks-uri: https://kubernetes.default.svc/openid/v1/jwks
  controllerManager: {}
  scheduler: {}
  etcd:
    extraArgs:
      listen-metrics-urls: http://0.0.0.0:2381
    advertiseClientURLs:
      - https://0.0.0.0:2379
    listenClientURLs:
      - https://0.0.0.0:2379
  discovery:
    registries:
      kubernetes: {}
      service: {}
  inlineManifests: []
  # После развёртывания поставьте CNI (например, Cilium/Calico) через talosctl apply -f ...